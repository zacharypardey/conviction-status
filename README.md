# Conviction · status

The public copy of the Conviction dashboard (a paper Bitcoin market maker on Kalshi's 15-minute windows). The page is static;
its data (`status.enc.json`) is encrypted with a passphrase and decrypted in the browser. Published every 15 minutes by the
`conviction` repo's `ops/publish.py`.
